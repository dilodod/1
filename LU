-- LocalScript в StarterPlayerScripts
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local localPlayer = Players.LocalPlayer

repeat task.wait() until localPlayer.Character
local character = localPlayer.Character
repeat task.wait() until character:FindFirstChild("HumanoidRootPart")
local hrp = character:WaitForChild("HumanoidRootPart")

-- === НАСТРОЙКИ ===
local SKILL_NAME = "Lethal Whirlwind Stream"
local CHECK_RADIUS = 3
local LIVE_FOLDER = "Live"
local COOLDOWN = 20
local SPIN_SPEED = 80
local RISE_HEIGHT = 3.5  -- На 3.5 стада выше цели во время Dash
local AIMBOT_DELAY = 0.2
local AIMBOT_DURATION = 0.3

local ANIM_SKILL_START = "rbxassetid://12296882427"

-- Цвета
local COLOR_COOLDOWN = Color3.fromRGB(60, 60, 60)
local COLOR_READY = Color3.fromRGB(100, 255, 100)
local COLOR_ANIMATION = Color3.fromRGB(0, 150, 255)
local COLOR_FORCEFIELD = Color3.fromRGB(150, 0, 255)
local COLOR_CASTING = Color3.fromRGB(255, 200, 0)
local COLOR_DASH = Color3.fromRGB(255, 150, 0)
local COLOR_AIMBOT = Color3.fromRGB(255, 50, 50)

-- === СОСТОЯНИЯ ===
local currentTarget = nil
local lastSkillTime = -COOLDOWN
local skillUsed = false
local hasForceField = false
local dashPending = false
local isSpinning = false
local isAiming = false

-- === GUI ===
local statusGui = Instance.new("ScreenGui")
statusGui.Name = "SkillStatus"
statusGui.ResetOnSpawn = false
statusGui.Parent = localPlayer:WaitForChild("PlayerGui")

local statusFrame = Instance.new("Frame")
statusFrame.Size = UDim2.new(0, 100, 0, 100)
statusFrame.Position = UDim2.new(0.5, -50, 0.75, 0)
statusFrame.BackgroundColor3 = COLOR_COOLDOWN
statusFrame.BorderSizePixel = 0

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 16)
corner.Parent = statusFrame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 255, 255)
stroke.Thickness = 4
stroke.Parent = statusFrame

local timerLabel = Instance.new("TextLabel")
timerLabel.Size = UDim2.new(1, 0, 0.6, 0)
timerLabel.Position = UDim2.new(0, 0, 0.1, 0)
timerLabel.BackgroundTransparency = 1
timerLabel.Text = "20"
timerLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
timerLabel.Font = Enum.Font.GothamBlack
timerLabel.TextSize = 48
timerLabel.Parent = statusFrame

local statusTextLabel = Instance.new("TextLabel")
statusTextLabel.Size = UDim2.new(1, 0, 0.3, 0)
statusTextLabel.Position = UDim2.new(0, 0, 0.7, 0)
statusTextLabel.BackgroundTransparency = 1
statusTextLabel.Text = "INIT"
statusTextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
statusTextLabel.Font = Enum.Font.GothamBold
statusTextLabel.TextSize = 16
statusTextLabel.Parent = statusFrame

statusFrame.Parent = statusGui

local function setColor(color, text)
    statusFrame.BackgroundColor3 = color
    statusTextLabel.Text = text
end

-- === ПОИСК ЦЕЛИ ===
local function getNearestTarget()
    local live = Workspace:FindFirstChild(LIVE_FOLDER)
    if not live then return nil end
    
    local nearest = nil
    local minDist = CHECK_RADIUS
    
    for _, obj in pairs(live:GetChildren()) do
        if obj ~= character and obj:IsA("Model") then
            local otherHrp = obj:FindFirstChild("HumanoidRootPart")
            if otherHrp then
                local dist = (hrp.Position - otherHrp.Position).Magnitude
                if dist < minDist then
                    minDist = dist
                    nearest = {
                        Model = obj,
                        HRP = otherHrp,
                        Distance = dist
                    }
                end
            end
        end
    end
    return nearest
end

local function checkForceField()
    if not currentTarget or not currentTarget.Model then return false end
    return currentTarget.Model:FindFirstChild("ForceField") ~= nil
end

-- === АИМБОТ НА ФИКСИРОВАННОЙ ВЫСОТЕ ===
local function startAimbotAtHeight(fixedY)
    if not currentTarget or not currentTarget.HRP then return end
    if isAiming then return end
    
    isAiming = true
    print("🎯 AIMBOT at Y=" .. string.format("%.1f", fixedY))
    setColor(COLOR_AIMBOT, "AIMBOT!")
    
    local startTime = tick()
    
    while isAiming do
        if tick() - startTime > AIMBOT_DURATION then
            isAiming = false
            break
        end
        
        if not currentTarget or not currentTarget.HRP then
            isAiming = false
            break
        end
        
        local targetPos = currentTarget.HRP.Position
        local myPos = hrp.Position
        
        -- На высоте +3.5, смотрим на цель
        hrp.CFrame = CFrame.new(
            Vector3.new(myPos.X, fixedY, myPos.Z),
            Vector3.new(targetPos.X, fixedY, targetPos.Z)
        )
        
        RunService.Heartbeat:Wait()
    end
    
    print("🎯 AIMBOT END")
    setColor(COLOR_READY, "READY")
end

-- === МЕГА ВЕРТУШКА (НА ОДНОЙ ВЫСОТЕ С ЦЕЛЬЮ) ===
local function megaSpinLoop()
    if isSpinning then return end
    isSpinning = true
    
    print("🌪️ SPIN — Same height as target")
    setColor(COLOR_FORCEFIELD, "SPIN!")
    
    while isSpinning do
        if not checkForceField() then
            print("⚠️ ForceField lost")
            isSpinning = false
            break
        end
        
        if not currentTarget or not currentTarget.HRP then
            isSpinning = false
            break
        end
        
        local elapsed = tick() - lastSkillTime
        local spinAngle = elapsed * SPIN_SPEED
        
        -- НА ОДНОЙ ВЫСОТЕ С ЦЕЛЬЮ
        local targetPos = currentTarget.HRP.Position
        local myPos = hrp.Position
        
        local targetDir = Vector3.new(targetPos.X - myPos.X, 0, targetPos.Z - myPos.Z).Unit
        local rotatedDir = CFrame.Angles(0, math.rad(spinAngle), 0) * targetDir
        
        -- Y = высота цели (ровно)
        hrp.CFrame = CFrame.new(
            Vector3.new(myPos.X, targetPos.Y, myPos.Z),
            Vector3.new(myPos.X, targetPos.Y, myPos.Z) + rotatedDir
        )
        
        RunService.Heartbeat:Wait()
    end
    
    print("🌪️ SPIN END")
end

-- === DASH+Q + ПОДЪЕМ НА 3.5 ===
local function triggerDashQ()
    if not currentTarget or not currentTarget.HRP then return end
    
    print("⚡ Dash+Q + Rise 3.5")
    setColor(COLOR_DASH, "DASH!")
    
    local targetPos = currentTarget.HRP.Position
    local myPos = hrp.Position
    local dashY = targetPos.Y + RISE_HEIGHT  -- На 3.5 выше цели
    
    -- Поднимаемся
    hrp.CFrame = CFrame.new(
        Vector3.new(myPos.X, dashY, myPos.Z),
        Vector3.new(targetPos.X, dashY, targetPos.Z)
    )
    hrp.Velocity = Vector3.new(0, 60, 0)
    
    print("🚀 Rise to Y=" .. string.format("%.1f", dashY))
    
    -- Dash+Q
    local communicate = character:FindFirstChild("Communicate")
    if communicate then
        local args = {
            [1] = {
                ["Dash"] = Enum.KeyCode.W,
                ["Key"] = Enum.KeyCode.Q,
                ["Goal"] = "KeyPress"
            }
        }
        pcall(function()
            communicate:FireServer(unpack(args))
        end)
        
        task.delay(0.1, function()
            local releaseArgs = {
                [1] = {
                    ["Dash"] = Enum.KeyCode.W,
                    ["Key"] = Enum.KeyCode.Q,
                    ["Goal"] = "KeyRelease"
                }
            }
            pcall(function()
                communicate:FireServer(unpack(releaseArgs))
            end)
        end)
    end
    
    -- Аимбот на высоте +3.5 через 0.2 сек
    task.delay(AIMBOT_DELAY, function()
        startAimbotAtHeight(dashY)
    end)
end

-- === АКТИВАЦИЯ СКИЛЛА ===
local function activateSkill()
    local backpack = localPlayer:WaitForChild("Backpack")
    local tool = backpack:FindFirstChild(SKILL_NAME) or character:FindFirstChild(SKILL_NAME)
    
    if not tool then return false end
    
    if tool.Parent == backpack then
        tool.Parent = character
        task.wait(0.1)
    end
    
    if tool.Parent == character then
        tool:Activate()
        lastSkillTime = tick()
        skillUsed = true
        dashPending = true
        print("✅ Skill activated")
        return true
    end
    
    return false
end

-- === ОТСЛЕЖИВАНИЕ ===
local function setupAnimationTracking()
    local humanoid = character:WaitForChild("Humanoid")
    local animator = humanoid:WaitForChild("Animator")
    
    animator.AnimationPlayed:Connect(function(track)
        local animId = track.Animation and track.Animation.AnimationId
        if animId == ANIM_SKILL_START then
            print("🔵 Animation detected")
            setColor(COLOR_ANIMATION, "ANIM!")
        end
    end)
end

-- === ГЛАВНЫЙ ЦИКЛ ===
print("✅ SCRIPT STARTED")

setupAnimationTracking()
setColor(COLOR_READY, "READY")

RunService.Heartbeat:Connect(function()
    if isSpinning or isAiming then return end
    
    local now = tick()
    local cdLeft = COOLDOWN - (now - lastSkillTime)
    
    timerLabel.Text = cdLeft > 0 and tostring(math.ceil(cdLeft)) or "GO"
    
    local ffNow = checkForceField()
    
    -- ForceField появился — вертушка на той же высоте
    if ffNow and not hasForceField then
        hasForceField = true
        print("🟣 ForceField — SPIN same height")
        task.spawn(megaSpinLoop)
        return
    end
    
    -- ForceField пропал — Dash с подъемом на 3.5
    if hasForceField and not ffNow then
        hasForceField = false
        isSpinning = false
        print("⚪ ForceField gone — Dash +3.5")
        
        if skillUsed and dashPending then
            triggerDashQ()
            skillUsed = false
            dashPending = false
        end
        return
    end
    
    -- Обычное состояние
    if ffNow then return end
    
    setColor(cdLeft > 0 and COLOR_COOLDOWN or COLOR_READY, cdLeft > 0 and "CD" or "READY")
    
    if cdLeft > 0 then return end
    
    currentTarget = getNearestTarget()
    if not currentTarget then 
        setColor(COLOR_COOLDOWN, "NO TARGET")
        return 
    end
    
    -- Поворачиваемся к цели
    local targetPos = currentTarget.HRP.Position
    local myPos = hrp.Position
    local dir = Vector3.new(targetPos.X - myPos.X, 0, targetPos.Z - myPos.Z).Unit
    hrp.CFrame = CFrame.new(myPos, myPos + dir)
    
    setColor(COLOR_CASTING, "CAST!")
    activateSkill()
end)

-- === СБРОС ===
localPlayer.CharacterAdded:Connect(function(newChar)
    character = newChar
    hrp = newChar:WaitForChild("HumanoidRootPart")
    
    currentTarget = nil
    lastSkillTime = -COOLDOWN
    skillUsed = false
    hasForceField = false
    dashPending = false
    isSpinning = false
    isAiming = false
    
    task.wait(0.5)
    setupAnimationTracking()
    setColor(COLOR_READY, "READY")
end)
